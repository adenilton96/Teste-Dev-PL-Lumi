
    1- migration sequelize OK

Rotas
    
    1- inserir dados das contas
        -“Numero DO CLIENTE”
        -“Mês de referência”
        -‘Energia Elétrica’– Quantidade (kWh) e Valor (R$),
        -‘Energia SCEEE s/ICMS’ – Quantidade (kWh) e Valor (R$),
        -‘Energia Compensada GD I’ – Quantidade (kWh) e Valor (R$),
        -‘Contrib Ilum Publica Municipal’ – Valor (R$),
        -salvar pdf em base64

    2- pegar os dados das contas ok
    3- fazer download das contas

    4- jest js para fazer os testes

    5- exportar pdf altomaticos 



 npm install pg express dotenv
 npm install dotenv sequelize pg

CREATE TABLE public.dados_fatura_energia
(
    id serial NOT NULL,
    numero_cliente bigint NOT NULL,
    mes_referencia date NOT NULL,
    eng_eletrica_qtd bigint NOT NULL,
    eng_eletrica_valor money NOT NULL,
    eng_sceee_ims_qtd bigint NOT NULL,
    eng_sceee_ims_valor money NOT NULL,
    eng_compensada_qtd bigint NOT NULL,
    eng_compensada_valor money NOT NULL,
    "contrib_Ilum_publica_valor" money NOT NULL,
    pdf_base64 "char" NOT NULL,
    PRIMARY KEY (id)
);

ALTER TABLE IF EXISTS public.dados_fatura_energia
    OWNER to postgres;



npx sequelize-cli db:create // cria o banco
npx sequelize-cli migration:generete dados_fatura_energia // CIRA MIGRATIONS
npx sequelize-cli db:migrate // far as migrate pu
npx sequelize-cli db:migrate:undo


